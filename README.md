# Microsserviço: ADM do Catálogo de vídeos com React e API Laravel

Run with

```bash
docker-compose up -d --build
```

```bash
docker exec -it micro-videos-app bash
```

```bash
cd app
```

```bash
npm start
```
